<% provide :title, @user.name %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <ul class="users follow">
        <%= render @user %>
      </ul>
    </section>
    <section class="stats">
      <%= render "shared/stats" %>
    </section>
  </aside>
</div>